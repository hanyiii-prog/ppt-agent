# V2.1 全量开发 · 批次进度（feature/v2.1-full）

> 本文件是跨会话续跑的状态载体。每个批次完成一行；新会话读此文件 + 对账文档即可续跑。
> 对账文档：仓库外（用户桌面）`V2.1_对账与重排_落地点重映射.md`。

## 执行红线（摘要）
1. `styling.resolve_layout()` 是两渲染器唯一布局算法——3.D 的 Solver 必须升级其内部，禁止另起第二份。
2. 核心 import 链零第三方依赖（CI `dependency-free-core` 门禁）；python-docx 只能进 extras。
3. 契约版本：`SUPPORTED_*_VERSIONS` 保留旧版可读，新输出盖新章。
4. 继承不重画 + 门禁显式降级——修复层扩展不得弱化。
5. 每批全量 pytest 全绿才提交；测试 fixtures 全部程序化构建，无二进制。
6. **LLM 兜底 + 强制披露**：sampling 不可用 → RuleFallback 兜底不中断；每次运行最终报告必须显式声明是否用到 LLM（`llm: sampled|fallback|off`），禁止静默降级。

## 批次状态

| 批 | 范围 | 状态 | commit | 测试 |
|---|---|---|---|---|
| 1 | 3.A 知识层：design_dna / semantic_role / element_library / spacing_graph / alignment_graph / design_rules / component_library + template-dna v1.0 契约 | ✅ 完成 | 2f63fa6..f3728a7（8 commits） | 355 → 380 全绿 |
| 2 | 3.B 内容层规则版：parsers/docx_to_ir、content_ir、content_analyzer(规则)、page_count、presentation_plan、narrative_engine(RuleFallback) | ✅ 完成 | 88436ea..2bd4ca7（5 commits） | 380 → 407 全绿 |
| 3 | LLM 基建：llm/provider.py 三件 + server.py 并发挂起升级 + llm 元数据披露 | ⬜ 未开始 | — | — |
| 4 | 3.C 生成层：page_archetype、component_matcher、fidelity_mode + 接 Presentation Plan | ⬜ 未开始 | — | — |
| 5 | 3.D 布局引擎：layout/ 六件 + resolve_layout 内部升级 | ⬜ 未开始 | — | — |
| 6 | 3.E 修复层：repair/ 七件 + 调度 | ⬜ 未开始 | — | — |
| 7 | 3.F 端到端：agent/pipeline.py + MCP 新工具 + 收官 V2.1.0 | ⬜ 未开始 | — | — |

## 批 1 交付明细
- 新建模块（全部零第三方依赖，纯 stdlib 聚合）：
  - `design_dna.py`：`build_design_dna()` 把 v0.4 deck dict 聚合为 v1.0（v0.4 全键保留），产出 shape/layout/typography/color 四段命名设计 DNA；母版占位符不参与 margins/safe-area（内容定位只看 slide-origin）。
  - `semantic_role.py`：15 类语义角色（title/logo/decoration/...），`infer_semantic_role()` 强信号优先规则，`annotate_deck_dna()` 全栈非破坏标注。
  - `element_library.py`：无名身份签名（element|prstGeom|fill|line|尺寸桶），`build_element_library()` 跨页复用元素 + reuse_score = 0.6·出现量 + 0.4·页覆盖。
  - `spacing_graph.py`：水平/垂直 gap 聚类（0.05in 网格，count≥2 才算 dominant）。
  - `alignment_graph.py`：左中右/上中下六轴对齐线聚类（0.06in 容差）。
  - `design_rules.py`：`build_design_rules()` 合成 design-rules/v1 规则书；`validate_design()` 三类检查 R-TYPO-001 / R-COLOR-001 / R-SPACING-001，发现必带 detail。
  - `component_library.py`：元素签名共现 + 并查集聚类 → 复用组件（≥2 签名、≥2 页、覆盖率≥0.7）。
- 契约：`contracts.py` 新增 `TEMPLATE_DNA_SCHEMA_VERSION="1.0"`、`SUPPORTED_TEMPLATE_DNA_VERSIONS=("0.4","1.0")`、`check_template_dna_version()`（解析容忍 `v` 前缀）、descriptor 扩展两字段。
- 测试：`tests/design_dna_fixtures.py` 程序化迷你模板（母版占位符为真材实料）+ 5 个测试文件 25 项。
- 已知取舍：typography scale 有序指派（display→caption 单调）；spacing dominant 需同值 gap ≥2 次；component 检测保守（避免误报），批 4 接 Component Matcher 时再增强。

## 批 2 交付明细（2026-09-18）
- `content_ir.py`：Content IR v0.1（6 类 block：heading/paragraph/bullets/ordered/table/quote），char_volume 容量模型（table 单元格≈12 CJK 字），sections() 分组，JSON 往返。
- `parsers/`：parse_markdown（纯 stdlib：标题/列表/表格/引用/围栏）、parse_docx（python-docx 懒导入）、parse_pptx（源 deck 文本抽取）、parse_auto 按扩展名分发。**新增 [docx] extras，核心链仍零依赖。**
- `content_analyzer.py`：每块 importance(0-1)/category/keywords（CJK 二字组+ASCII 词，停用词表），非破坏增强，metadata `analyzer: rules, llm: off`。
- `page_count.py`：固定开销（cover+toc+closing）+ 章节字容量分页（260 字/标准页 × 密度系数 compact 1.3 / air 0.75），表格独占页；估算必带 assumptions。
- `presentation_plan.py`：kind 标注页计划（cover/toc/section/content/closing）+ source_blocks 双向可追溯，表格独占页，与 page_count 对账（容差 ≤3）。
- `narrative_engine.py`：**LLM seam（llm_fn 参数）+ 强制规则兜底**——off/sampled/fallback 三态披露（红线 6 已在此落地）；规则版按中文关键词桶分五段叙事弧（context/action/evidence/outlook/risk），specific 优先于 general 的桶序；硬指标判定（百分比/多位数）驱动 data/narrative emphasis。
- 契约：contracts.py 增 `CONTENT_IR_SCHEMA_VERSION="0.1"` + `check_content_ir_version()`；descriptor 两字段。
- 设计决策：H1=封面材料不参与 sections 计数；story.py 未改动（基线安全），narrative_engine 独立实现规则弧；批 3 将把 llm_fn 接到 MCPSamplingProvider。

## 批 3 入场提示（新会话从这里继续）
- `llm/provider.py`：LLMProvider 协议 + MCPSamplingProvider（走 server session 反向 createMessage，60s 超时+重试 1）+ RuleFallbackProvider + NullProvider；`llm_fn` seam 由 narrative_engine 消费。
- `mcp/server.py` 主循环从"单向读 stdin"升级为可并发挂起等 sampling 响应（JSON-RPC id 关联）；握手读 `capabilities.sampling` 判定，不支持→自动降级并在 metadata 披露。
- 完成后更新本表批 3 行 + 全量测试 + MCP push_files + 批次报告。
